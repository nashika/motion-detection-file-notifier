// Generated by CoffeeScript 1.10.0
(function() {
  var Notifier, Pushbullet, PushbulletNotifier, core,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  Pushbullet = require('pushbullet');

  core = require('./core');

  Notifier = require('./notifier');

  PushbulletNotifier = (function(superClass) {
    extend(PushbulletNotifier, superClass);


    /**
     * @protected
     * @type Pushbullet
     */

    PushbulletNotifier.prototype._pushbullet = null;


    /**
     * @override
     */

    function PushbulletNotifier() {
      this.notify = bind(this.notify, this);
      PushbulletNotifier.__super__.constructor.call(this);
      if (core.commander.apikey) {
        this._pushbullet = new Pushbullet(core.commander.apikey);
        this._pushbullet.me((function(_this) {
          return function(err, response) {
            if (err) {
              core.logger.fatal('Pushbullet auth failed.');
              return process.exit(1);
            } else {
              return core.logger.info("Pushbullet auth succeed. response=" + (JSON.stringify(response)));
            }
          };
        })(this));
      } else {
        core.logger.fatal('Pushbullet notifier need --apikey option.');
        process.exit(1);
      }
    }


    /**
     * @override
     */

    PushbulletNotifier.prototype.notify = function(message) {
      PushbulletNotifier.__super__.notify.call(this, message);
      return this._pushbullet.note(null, message, message, (function(_this) {
        return function(err) {};
      })(this));
    };

    return PushbulletNotifier;

  })(Notifier);

  module.exports = PushbulletNotifier;

}).call(this);

//# sourceMappingURL=pushbullet-notifier.js.map
